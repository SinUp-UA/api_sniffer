# Universal API Sniffer

**Универсальное расширение для перехвата API запросов** - работает в Chrome, Firefox, Edge, Opera и других браузерах на базе Chromium.

> 👨‍💻 **Автор**: SinUP Agency | [GitHub](https://github.com/SinUp-UA)

## 📑 Содержание

- [Возможности](#-возможности)
- [Быстрый старт](#-быстрый-старт)
- [Установка](#-установка)
- [Использование](#-использование)
- [Структура проекта](#-структура-проекта)
- [Технические детали](#-технические-детали)
- [Форматы экспорта](#-форматы-экспорта)
- [Разработка](#-разработка)
- [Отчёт об универсализации](#-отчёт-об-универсализации)
- [Работа с иконками](#-работа-с-иконками)
- [Устранение неполадок](#-устранение-неполадок)
- [Автообновление](#-автообновление)
- [Поддержка](#-поддержка)

## 🎯 Возможности

- ✅ Перехват **fetch** запросов
- ✅ Перехват **XMLHttpRequest (XHR)**
- ✅ Мониторинг **WebSocket** соединений
- ✅ Отслеживание **Server-Sent Events (SSE)**
- ✅ Экспорт логов в **JSON** формате
- ✅ Экспорт в **ZIP** архив (с JSON, HAR, summary, meta)
- ✅ Запись и остановка перехвата в реальном времени
- ✅ Предпросмотр последних запросов
- ✅ Полностью универсальный код для всех браузеров
- ✅ **Кроссплатформенность**: Windows, macOS, Linux

## 🖥️ Поддержка операционных систем

Расширение **полностью кроссплатформенное** и работает на всех операционных системах:

- ✅ **Windows** 10/11
- ✅ **macOS** 10.12 и выше
- ✅ **Linux** (Ubuntu, Fedora, Debian, Arch Linux и др.)

**Почему?** Браузерные расширения работают на уровне JavaScript в браузере и не зависят от операционной системы. Код использует только стандартные Web API и Browser Extension API.

## 🌐 Быстрый старт

### Для Chrome / Edge / Opera / Brave

1. Откройте браузер
2. Введите в адресной строке:
   - Chrome: `chrome://extensions/`
   - Edge: `edge://extensions/`
   - Opera: `opera://extensions/`
   - Brave: `brave://extensions/`
   - Vivaldi: `vivaldi://extensions/`
3. Включите **"Режим разработчика"** (переключатель справа вверху)
4. Нажмите **"Загрузить распакованное расширение"**
5. Выберите папку **`chrome`** из этого проекта
6. Готово! Иконка расширения появится на панели инструментов

**💡 Работает на**: Windows, macOS, Linux

### Для Firefox

1. Откройте Firefox
2. Введите в адресной строке: `about:debugging#/runtime/this-firefox`
3. Нажмите **"Загрузить временное дополнение..."**
4. Перейдите в папку **`firefox`** и выберите файл **`manifest.json`**
5. Готово! Иконка расширения появится на панели инструментов

**⚠️ Примечание**: В Firefox временное расширение работает до перезапуска браузера.

**💡 Работает на**: Windows, macOS, Linux

### Первые шаги

1. **Кликните** на иконку расширения
2. **Нажмите** "Включить запись"
3. **Обновите** любую веб-страницу или выполните действия на сайте
4. **Просмотрите** перехваченные API запросы в окне расширения
5. **Экспортируйте** данные:
   - Кнопка "Экспорт JSON" - простой JSON файл
   - Кнопка "Экспорт ZIP" - архив с JSON, HAR, summary и meta файлами

### Что перехватывается?

- ✅ `fetch()` запросы
- ✅ `XMLHttpRequest` (AJAX)
- ✅ `WebSocket` соединения
- ✅ `EventSource` (Server-Sent Events)

## �📦 Структура проекта

```
api_sniffer/
├── chrome/          # Версия для Chrome/Edge/Opera (Manifest V3)
├── firefox/         # Версия для Firefox (Manifest V2)
├── icons/           # Общие иконки (SVG)
├── background.js    # Универсальный background script
├── content.js       # Универсальный content script
├── popup.js         # Универсальный popup script
├── popup.html       # Интерфейс popup
├── popup.css        # Стили popup
├── sniffer.js       # Код для перехвата API
├── manifest.json    # Базовый манифест (MV3)
└── README.md        # Документация
```

## 🚀 Установка

### Chrome / Edge / Opera / Brave (Chromium-браузеры)

1. Скачайте или клонируйте репозиторий
2. Откройте браузер и перейдите:
   - **Chrome**: `chrome://extensions/`
   - **Edge**: `edge://extensions/`
   - **Opera**: `opera://extensions/`
   - **Brave**: `brave://extensions/`
3. Включите **"Режим разработчика"** (Developer mode) в правом верхнем углу
4. Нажмите **"Загрузить распакованное расширение"** (Load unpacked)
5. Выберите папку `chrome/` из проекта
6. Расширение установлено! ✅

### Firefox

1. Скачайте или клонируйте репозиторий
2. Откройте Firefox и перейдите: `about:debugging#/runtime/this-firefox`
3. Нажмите **"Загрузить временное дополнение..."** (Load Temporary Add-on)
4. Перейдите в папку `firefox/` и выберите файл `manifest.json`
5. Расширение установлено! ✅

**Примечание**: В Firefox временные расширения работают только до перезапуска браузера. Для постоянной установки нужно подписать расширение через [AMO](https://addons.mozilla.org/).

### Safari (macOS)

Для Safari требуется конвертация расширения с помощью Xcode и специальной утилиты. Процесс более сложный и требует:
1. Установку Xcode
2. Использование `xcrun safari-web-extension-converter`
3. Подписание приложения с Apple Developer ID

## 📖 Использование

1. **Откройте расширение**: Кликните на иконку расширения в панели браузера
2. **Включите запись**: Нажмите кнопку "Включить запись"
3. **Работайте с сайтами**: Все API запросы будут перехвачены
4. **Просмотр логов**: В окне расширения видны последние 5 запросов
5. **Экспорт данных**:
   - **JSON**: Экспорт всех логов в формате JSON
   - **ZIP**: Экспорт архива с JSON, HAR, summary и метаданными
6. **Очистка**: Кнопка "Очистить логи" удаляет все записанные данные

### Полезные команды

**Очистить логи** - удаляет все записанные данные

**Остановить запись** - прекращает перехват без удаления данных

**Предпросмотр** - внизу окна показаны последние 5 перехваченных запросов

**Совет**: Откройте консоль разработчика (F12) для просмотра подробных логов расширения при отладке

## 🔧 Технические детали

### Поддерживаемые API

- **fetch()** - современный API для HTTP запросов
- **XMLHttpRequest** - классический XHR API
- **WebSocket** - двусторонняя связь
- **EventSource** - Server-Sent Events (SSE)

### Универсальность

Расширение использует универсальную обёртку для работы с API браузера:

```javascript
const browserAPI = typeof browser !== 'undefined' ? browser : chrome;
```

Это позволяет коду работать как в Firefox (который использует `browser.*`), так и в Chrome/Chromium (которые используют `chrome.*`).

### Различия манифестов

- **Chrome/Chromium**: Manifest V3 (современная версия)
  - Использует `service_worker` вместо фоновых скриптов
  - Требует явного указания `host_permissions`
  - Более строгие правила безопасности

- **Firefox**: Manifest V2 (пока основная версия)
  - Использует `background.scripts`
  - Поддерживает `browser.*` API нативно
  - Требует `browser_specific_settings` для ID расширения

## 📊 Форматы экспорта

### JSON
Простой массив всех перехваченных событий с полной информацией:
- URL, метод, заголовки
- Тела запросов и ответов
- Временные метки
- Статусы ответов

### HAR (HTTP Archive)
Стандартный формат для анализа HTTP трафика, совместимый с:
- Chrome DevTools
- Firefox Developer Tools
- HAR Viewer
- Другими инструментами анализа

### Summary
Статистика по перехваченным запросам:
- Общее количество событий
- Группировка по endpoints
- Частота вызовов

## 🛠️ Разработка

### Требования
- Любой современный браузер для тестирования
- Текстовый редактор или IDE

### Модификация
1. Внесите изменения в корневые файлы (`background.js`, `content.js`, `popup.js`)
2. Скопируйте изменённые файлы в папки `chrome/` и `firefox/`
3. Перезагрузите расширение в браузере:
   - **Chrome**: На странице расширений нажмите кнопку "Обновить"
   - **Firefox**: Нажмите "Перезагрузить" на странице отладки

### Добавление новых браузеров

Расширение уже универсально и должно работать в:
- ✅ Google Chrome
- ✅ Microsoft Edge
- ✅ Mozilla Firefox
- ✅ Opera
- ✅ Brave
- ✅ Vivaldi
- ⚠️ Safari (требует дополнительной конвертации)

## � Отчёт об универсализации

### Выполненные изменения

#### 1. Универсализация кода JavaScript

**Проблема**: Код использовал только `chrome.*` API, что не работало в Firefox.

**Решение**: Добавлена универсальная обёртка во все JS файлы:
```javascript
const browserAPI = typeof browser !== 'undefined' ? browser : chrome;
```

**Затронутые файлы**:
- ✅ `background.js` - полностью переписан с `browserAPI`
- ✅ `content.js` - полностью переписан с `browserAPI`
- ✅ `popup.js` - полностью переписан с `browserAPI`
- ✅ `sniffer.js` - не требует изменений (работает в контексте страницы)

#### 2. Обновление манифестов

**Chrome/Chromium** (`chrome/manifest.json`):
- ✅ Manifest V3 (современная версия)
- ✅ Удалено лишнее разрешение `scripting` (не использовалось)
- ✅ Использует `service_worker`
- ✅ Правильные `web_accessible_resources` для MV3

**Firefox** (`firefox/manifest.json`):
- ✅ Manifest V2 (основная версия для Firefox)
- ✅ Использует `background.scripts`
- ✅ Правильный `browser_specific_settings` с gecko ID
- ✅ Минимальная версия Firefox: 57.0
- ✅ Упрощённый `web_accessible_resources` для MV2

**Корневой** (`manifest.json`):
- ✅ Базовый MV3 манифест для быстрого тестирования

#### 3. Синхронизация файлов

Все универсальные JavaScript файлы скопированы в папки:
- ✅ `chrome/` - для Chromium браузеров
- ✅ `firefox/` - для Firefox

### Поддерживаемые браузеры

#### ✅ Полная поддержка
- **Google Chrome** 88+ (Manifest V3)
- **Microsoft Edge** 88+ (Manifest V3)
- **Opera** 74+ (Manifest V3)
- **Brave** (Manifest V3)
- **Vivaldi** (Manifest V3)
- **Yandex Browser** (Manifest V3)
- **Mozilla Firefox** 57+ (Manifest V2)
- **Любые Chromium-браузеры** с поддержкой Manifest V3

**💡 Все браузеры работают на Windows, macOS и Linux**

#### ⚠️ Требует дополнительных действий
- **Safari** - требует конвертации через Xcode

### Ключевые преимущества

1. **Единая кодовая база**: JavaScript код полностью идентичен для всех браузеров
2. **Автоматическое определение API**: Код сам определяет, какой API использовать
3. **Правильные манифесты**: Отдельные оптимизированные манифесты для MV2 и MV3
4. **Простота поддержки**: Изменения делаются в корневых файлах, затем копируются

## 🎨 Работа с иконками

### Автоматическая генерация (рекомендуется)

Используйте PowerShell скрипт для автоматического создания всех PNG иконок:

```powershell
powershell -ExecutionPolicy Bypass -File create-icons.ps1
```

Скрипт автоматически создаст все необходимые PNG файлы (16x16, 32x32, 48x48, 128x128) в папках `chrome/icons/`, `firefox/icons/` и `icons/`.

### Альтернативные методы

#### Метод 1: Использование generate-icons.html
1. Откройте файл `generate-icons.html` в браузере
2. Файлы 16.png, 32.png, 48.png, 128.png автоматически загрузятся
3. Переместите их в папки `chrome/icons/` и `firefox/icons/`

#### Метод 2: Онлайн конвертер
1. Откройте файл `icons/icon.svg` в любом SVG редакторе или онлайн конвертере
2. Экспортируйте в размерах: 16x16, 32x32, 48x48, 128x128 пикселей
3. Сохраните как 16.png, 32.png, 48.png, 128.png
4. Скопируйте в папки `chrome/icons/` и `firefox/icons/`

#### Метод 3: Использование ImageMagick (если установлен)
```bash
cd icons
magick icon.svg -resize 16x16 ../chrome/icons/16.png
magick icon.svg -resize 32x32 ../chrome/icons/32.png
magick icon.svg -resize 48x48 ../chrome/icons/48.png
magick icon.svg -resize 128x128 ../chrome/icons/128.png
# Повторить для firefox/icons/
```

### Проверка иконок

После создания иконок убедитесь, что файлы имеют правильный размер:

```powershell
Get-ChildItem chrome\icons\*.png | Select-Object Name, Length
```

Правильные размеры: **300-900 байт** для каждого файла. Если размер ~8 байт - файлы повреждены!

## � Автообновление

Расширение поддерживает автоматическое обновление через GitHub Releases.

### Как это работает

1. При создании нового релиза в GitHub (тег `v1.0.1`, `v1.0.2` и т.д.)
2. GitHub Action автоматически создает пакеты для Chrome и Firefox
3. Браузеры пользователей периодически проверяют обновления
4. Новая версия устанавливается автоматически

### Частота проверки обновлений

- **Chrome/Edge/Opera**: каждые 5 часов
- **Firefox**: каждые 24 часа
- **Вручную**: в настройках расширений браузера

### Конфигурация

- `updates.xml` - файл обновлений для Chrome/Chromium
- `updates.json` - файл обновлений для Firefox
- `.github/workflows/release.yml` - автоматизация релизов

### Создание нового релиза

```bash
# 1. Обновите версию в manifest.json (chrome/ и firefox/)
# 2. Закоммитьте изменения
git add .
git commit -m "Version 1.0.1"
git push

# 3. Создайте и отправьте тег
git tag v1.0.1
git push origin v1.0.1

# GitHub Action автоматически создаст ДВА отдельных релиза:
# - chrome-v1.0.1 для Chrome/Edge/Opera/Brave
# - firefox-v1.0.1 для Firefox
```

**Подробная инструкция**: см. [RELEASE_GUIDE.md](RELEASE_GUIDE.md)

⚠️ **Важно**: Self-hosted обновления работают только в режиме разработчика. Для production рекомендуется публикация в официальных магазинах расширений.

## �🐛 Устранение неполадок

### Расширение не отображается
- Убедитесь, что режим разработчика включён
- Проверьте, что выбрана правильная папка (chrome/ или firefox/)

### Логи не записываются
- Убедитесь, что запись включена (кнопка "Включить запись")
- Проверьте консоль браузера на наличие ошибок

### Экспорт не работает
- Проверьте разрешения браузера на загрузку файлов
- Убедитесь, что в логах есть данные

## � Настройка автообновления через GitHub

### Предварительные требования

1. Репозиторий размещен на GitHub: https://github.com/SinUp-UA/api_sniffer
2. Включены GitHub Releases в настройках репозитория
3. Включены GitHub Actions

### Как это работает

#### Автоматический процесс:

1. **Создаете новый тег** (например, `v1.0.1`)
2. **GitHub Action автоматически:**
   - Создает ZIP для Chrome
   - Создает XPI для Firefox
   - Обновляет `updates.xml` и `updates.json`
   - Публикует новый Release
   - Загружает пакеты в Release

3. **Браузеры пользователей:**
   - Периодически проверяют `updates.xml` / `updates.json`
   - Автоматически скачивают и устанавливают обновление

### Инструкция по созданию релиза

#### Способ 1: Через командную строку

```bash
# 1. Убедитесь, что все изменения закоммичены
git add .
git commit -m "Prepare version 1.0.1"
git push

# 2. Создайте и отправьте тег
git tag v1.0.1
git push origin v1.0.1

# 3. GitHub Action автоматически создаст релиз
```

#### Способ 2: Через GitHub веб-интерфейс

1. Перейдите в репозиторий на GitHub
2. Откройте **Releases** → **Draft a new release**
3. Нажмите **Choose a tag** → Введите новый тег (например, `v1.0.1`)
4. Нажмите **Create new tag**
5. Заполните название и описание релиза
6. Нажмите **Publish release**
7. GitHub Action запустится автоматически

### Конфигурация

**Файлы автообновления:**
- `updates.xml` - для Chrome/Edge/Opera/Brave
- `updates.json` - для Firefox
- `.github/workflows/release.yml` - автоматизация

**Манифесты обновлены:**
- **Chrome**: добавлен `update_url` в `chrome/manifest.json`
- **Firefox**: добавлен `update_url` в `firefox/manifest.json`

### Как проверить работу автообновления

**Для Chrome:**

1. Установите расширение
2. Откройте `chrome://extensions/`
3. Включите "Режим разработчика"
4. Нажмите "Обновить расширения сейчас"
5. Chrome проверит `updates.xml` и установит новую версию

**Для Firefox:**

1. Установите расширение
2. Откройте `about:addons`
3. Нажмите на шестеренку → "Проверить обновления"
4. Firefox проверит `updates.json` и установит новую версию

### Важные замечания

**ID расширения для Chrome:**

При первой установке Chrome назначает уникальный ID. Чтобы автообновления работали:

1. Установите расширение в режиме разработчика
2. Скопируйте Extension ID из `chrome://extensions/`
3. Замените `YOUR_EXTENSION_ID_HERE` в `updates.xml` на реальный ID
4. Закоммитьте изменения

**Или** используйте фиксированный ID через `key` в manifest.json:

```json
{
  "key": "YOUR_PUBLIC_KEY_HERE",
  "update_url": "https://raw.githubusercontent.com/SinUp-UA/api_sniffer/main/updates.xml"
}
```

**Частота проверки обновлений:**
- **Chrome**: проверяет каждые 5 часов
- **Firefox**: проверяет каждые 24 часа
- **Можно проверить вручную** в настройках браузера

**Ограничения:**

⚠️ **Self-hosted обновления работают только в режиме разработчика!**

Для production-версии необходимо:
- **Chrome**: публикация в Chrome Web Store
- **Firefox**: подпись через Firefox Add-ons (может быть unlisted)

### Рекомендации для production

**Вариант 1: Публикация в магазинах (рекомендуется)**

- ✅ Автообновления работают для всех пользователей
- ✅ Не требуется режим разработчика
- ✅ Больше доверия от пользователей
- ❌ Chrome Web Store: $5 единоразово
- ❌ Требуется модерация (1-3 дня)

**Вариант 2: Self-hosted (текущая конфигурация)**

- ✅ Полный контроль над распространением
- ✅ Подходит для внутренних/корпоративных нужд
- ❌ Требуется режим разработчика
- ❌ Предупреждения браузера о непроверенном расширении

### Дополнительные ресурсы

- [Chrome Extension Update](https://developer.chrome.com/docs/extensions/mv3/hosting/)
- [Firefox Extension Update](https://extensionworkshop.com/documentation/manage/updating-your-extension/)
- [GitHub Actions Documentation](https://docs.github.com/en/actions)

### Устранение неполадок

**Автообновление не работает:**

1. Проверьте, что URL в манифесте корректен
2. Убедитесь, что `updates.xml`/`updates.json` доступны по URL
3. Проверьте версию в манифесте (должна быть меньше версии в update-файле)
4. Проверьте логи браузера (F12 на странице расширений)

**GitHub Action не запускается:**

1. Убедитесь, что Actions включены в репозитории
2. Проверьте, что тег создан правильно (`v1.0.0`, а не `1.0.0`)
3. Проверьте права доступа GitHub Token в настройках репозитория

---

## 🚀 Инструкция по созданию релизов

### Настройка завершена!

Теперь система создает **отдельные релизы** для Chrome и Firefox.

### Как создать новый релиз

**Простой способ (один тег):**

```bash
# 1. Обновите версию в manifest.json (в папках chrome/ и firefox/)
# 2. Закоммитьте изменения
git add .
git commit -m "Version 1.0.1: Bug fixes"
git push

# 3. Создайте тег версии (без префикса браузера)
git tag v1.0.1
git push origin v1.0.1
```

**Результат**: GitHub Action автоматически создаст **три отдельных релиза**:
- `chrome-v1.0.1` с файлом для Chrome (только Chrome)
- `firefox-v1.0.1` с файлом для Firefox
- `chromium-v1.0.1` с файлом для Edge/Opera/Brave/Vivaldi и других Chromium браузеров

### Что создается автоматически

**Для Chrome:**
- **Тег**: `chrome-v1.0.1`
- **Релиз**: "Chrome/Edge/Opera/Brave v1.0.1"
- **Файл**: `Universal-API-Sniffer-Chrome-v1.0.1.zip`
- **Описание**: Инструкции по установке для Chrome

**Для Firefox:**
- **Тег**: `firefox-v1.0.1`
- **Релиз**: "Firefox v1.0.1"
- **Файл**: `Universal-API-Sniffer-Firefox-v1.0.1.xpi`
- **Описание**: Инструкции по установке для Firefox

**Для других Chromium браузеров:**
- **Тег**: `chromium-v1.0.1`
- **Релиз**: "Edge/Opera/Brave/Vivaldi v1.0.1"
- **Файл**: `Universal-API-Sniffer-Chromium-v1.0.1.zip`
- **Описание**: Инструкции для Edge, Opera, Brave, Vivaldi, Yandex Browser и других

**Автообновление файлов:**
- `updates.xml` обновляется ссылкой на Chrome релиз
- `updates.json` обновляется ссылкой на Firefox релиз

### Проверка релизов

После создания тега подождите 1-2 минуты и проверьте:

1. **GitHub Actions**: https://github.com/SinUp-UA/api_sniffer/actions
2. **Releases**: https://github.com/SinUp-UA/api_sniffer/releases

Вы увидите три отдельных релиза с красивыми описаниями и информацией о поддержке платформ!

### Пример процесса обновления

```bash
# Текущая версия: 1.0.0
# Новая версия: 1.0.1

# 1. Обновите версию в файлах
# chrome/manifest.json: "version": "1.0.1"
# firefox/manifest.json: "version": "1.0.1"

# 2. Закоммитьте
git add chrome/manifest.json firefox/manifest.json
git commit -m "Bump version to 1.0.1"
git push

# 3. Создайте тег
git tag v1.0.1
git push origin v1.0.1

# 4. Ждите создания релизов (1-2 минуты)
```

### Преимущества

✅ Пользователи Chrome видят только релиз для Chrome
✅ Пользователи Firefox видят только релиз для Firefox
✅ Пользователи Edge/Opera/Brave видят релиз для Chromium браузеров
✅ Каждый релиз имеет специфичные инструкции
✅ Файлы имеют понятные названия
✅ Автообновление работает корректно для каждого браузера
✅ Указана поддержка всех операционных систем (Windows, macOS, Linux)

---

## �📝 Лицензия

© 2025 SinUP Agency. Все права защищены.

Данное программное обеспечение является проприетарным. Использование, копирование, распространение или модификация без письменного разрешения автора запрещены.

## 🤝 Вклад

По вопросам сотрудничества и предложений обращайтесь к автору через GitHub.

## 📞 Поддержка

При возникновении проблем:
1. Проверьте консоль браузера (F12)
2. Убедитесь, что используете последнюю версию
3. Создайте issue с описанием проблемы

---

## 👨‍💻 Автор

**SinUP Agency**  
GitHub: [https://github.com/SinUp-UA](https://github.com/SinUp-UA)

---

**Версия**: 1.0.0  
**Совместимость**: Chrome 88+, Firefox 57+, Edge 88+, Opera 74+  
**Лицензия**: Proprietary (Проприетарная)
